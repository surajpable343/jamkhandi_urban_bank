import 'package:flutter/cupertino.dart';

class LoanCalulatorPage extends StatelessWidget {
  const LoanCalulatorPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
